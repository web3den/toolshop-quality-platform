# Target environment: production (default) | with-bugs | local
TARGET=production

# Only needed when TARGET=local (docker compose from testsmith-io/practice-software-testing)
LOCAL_UI_URL=http://localhost:4200
LOCAL_API_URL=http://localhost:8091
